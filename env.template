# Copy this file to .env and update with your actual values

# ===========================================
# SmartThings API Configuration
# ===========================================
# Get your token from: https://account.smartthings.com/tokens
SMARTTHINGS_TOKEN=your_personal_access_token_here

# Device IDs - find these by running: node scripts/discover-devices.js
WASHER_DEVICE_ID=your_washer_device_id
DRYER_DEVICE_ID=your_dryer_device_id

# ===========================================
# Server Configuration
# ===========================================
PORT=3000
HTTPS_PORT=3443
NODE_ENV=production

# HTTPS/SSL Configuration
# Enable HTTPS (requires SSL certificates)
ENABLE_HTTPS=false
# SSL certificate paths (inside container if using Docker)
SSL_KEY_PATH=./certs/key.pem
SSL_CERT_PATH=./certs/cert.pem

# ===========================================
# Optional Settings
# ===========================================
# How often to refresh device status (milliseconds)
REFRESH_INTERVAL=30000

# API request timeout (milliseconds)
API_TIMEOUT=10000

# Custom device display names
WASHER_NAME=Washer
DRYER_NAME=Dryer

# ===========================================
# CORS Configuration
# ===========================================
# Allowed origins for CORS (comma-separated)
# Examples:
# For specific domains: ALLOWED_ORIGINS=https://your-domain.com,https://your-subdomain.your-domain.com
# For wildcard subdomains: ALLOWED_ORIGINS=https://*.your-domain.com
# For development (any origin): ALLOWED_ORIGINS=*
# For local network access: ALLOWED_ORIGINS=http://192.168.1.*,http://10.0.0.*
ALLOWED_ORIGINS=http://localhost:3000
